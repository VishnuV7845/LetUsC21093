/*
Name: Vishnu Vignesh
Roll No.: CH.EN.U4CYS21093
*/

#include <stdio.h>
#include <math.h>

int main() {
    int x, y;

    printf("Number = ");
    scanf("%d", &x);

    // Use abs() to calculate absolute value
    y = abs(x);
    printf("The absolute value of %d is %d.", x, y);

    return 0;
}